const Discord = require('discord.js');

exports.run = (client, message, args) => {
 const user = message.mentions.users.first();
 // The mentioned user.
 member.ban();
 // Bans the user
 console.log(`${member.user.tag} was banned!`)
 // Logs when the user is banned.
}
